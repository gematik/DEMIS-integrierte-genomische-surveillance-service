{{- if not .Values.istio.enable }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "integrierte-genomische-surveillance-service.fullname" . }}
  labels:
    {{- include "integrierte-genomische-surveillance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.http }}
      targetPort: {{ .Values.service.ports.http }}
      protocol: TCP
      name: http
  selector:
    {{- include "integrierte-genomische-surveillance-service.selectorLabels" . | nindent 4 }}
{{- end }}